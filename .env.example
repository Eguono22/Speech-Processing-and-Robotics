# Flask session signing key
SECRET_KEY=change-me-in-production

# Optional custom path for persistent state DB
# STATE_DB_PATH=instance/robot_state.db

# Flask debug mode (1=true, 0=false)
FLASK_DEBUG=0
